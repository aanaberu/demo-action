name: Java CY with Gradel

on:
  push:
    branches:
    - main
    - dev
  workflow_dispatch:
    inputs:
      environment:
        type: string
        required: true
      branch:
        type: string
        required: true
